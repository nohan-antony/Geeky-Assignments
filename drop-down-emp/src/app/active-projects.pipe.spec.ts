import { ActiveProjectsPipe } from './active-projects.pipe';

describe('ActiveProjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveProjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
